<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Subscription Confirmation</title>
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/footer.css">
    <style>
        /* Global body styling */
        body {
            font-family: 'Roboto', Arial, sans-serif;
            background-color: #f9f9fb; /* Softer background */
            color: #333; /* Dark gray text */
            margin: 0;
            padding: 0;
        }

        /* Container styling */
        .container {
            background-color: #fff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1); /* Softer shadow */
            max-width: 600px;
            margin: 60px auto; /* Centered container */
            text-align: center; /* Center-align content */
        }

        /* Headings */
        h1 {
            font-size: 28px;
            color: #007bff; /* Vibrant blue */
            margin-bottom: 20px;
        }

        /* Details section */
        .details {
            text-align: left; /* Align details to the left */
            margin: 20px 0;
        }
        .details p {
            font-size: 16px;
            line-height: 1.6;
            margin: 8px 0;
        }
        .btn {
        display: inline-block;
        padding: 10px 20px;
        font-size: 16px;
        font-weight: bold;
        color: white;
        background-color: #007bff; /* Blue button */
        text-decoration: none;
        text-align: center;
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

    .btn:hover {
        background-color: #0056b3; /* Darker blue on hover */
    }

    .back-button {
        margin-top: 20px;
        display: block; /* Ensures it appears as a block and not inline */
        text-align: center; /* Centers the button in its container */
        width: 200px; /* Set a fixed width for consistency */
        margin: 20px auto; /* Center horizontally */
    }
    </style>
</head>
<body>
            <%- include('nav') %>

    <main>
    <div>
        <p><strong>Product ID:</strong> <%= orderDetails.productId %></p>
        <p><strong>Price:</strong> ₹<%= orderDetails.price %></p>
        <p><strong>Name:</strong> <%= orderDetails.name %></p>
        <p><strong>Address:</strong> <%= orderDetails.address %></p>
    </div>

<a href="/active-subscriptions" class="btn back-button">Back to Subscriptions</a>
</main>
<footer class="footer">
    <div class="footer-container">
        <div class="footer-section">
            <h3>FreshMart</h3>
            <p>Bringing fresh produce directly from farms to your table.</p>
        </div>
        <div class="footer-section">
            <h3>Quick Links</h3>
            <ul>
                <li><a href="products.html">Products</a></li>
                <li><a href="careers.html">Careers</a></li>
                <li><a href="contact.html">Contact</a></li>
                <li><a href="faqs.html">FAQs</a></li>
            </ul>
        </div>
        <div class="footer-section">
            <h3>Follow Us</h3>
            <div class="social-links">
                <a href="#" class="social-link">Facebook</a>
                <a href="#" class="social-link">Instagram</a>
                <a href="#" class="social-link">Twitter</a>
            </div>
        </div>
    </div>
    <div class="footer-bottom">
        <p>&copy; 2024 FreshMart. All rights reserved.</p>
    </div>
</footer>
</body>
</html>
