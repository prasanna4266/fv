<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Subscription Details</title>
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/footer.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 50px auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        h1, h2, h3 {
            color: #333;
            text-align: center;
        }

        p {
            color: #666;
            font-size: 16px;
            margin: 10px 0;
        }

        .actions {
            margin-top: 20px;
            text-align: center;
        }

        .btn {
            display: inline-block;
            padding: 12px 25px;
            font-size: 16px;
            font-weight: bold;
            color: white;
            background-color: #007bff; /* Vibrant blue */
            text-decoration: none;
            border: none;
            border-radius: 8px;
            transition: all 0.3s ease; /* Smooth hover transition */
            cursor: pointer;
            box-shadow: 0 4px 10px rgba(0, 123, 255, 0.3); /* Button shadow */
        }

        .btn:hover {
            background-color: #0056b3;
            box-shadow: 0 6px 15px rgba(0, 123, 255, 0.5); /* Intense shadow on hover */
        }
        
    </style>
</head>
<body>
            <%- include('nav') %>

    <main>
    <h1>Subscription Details</h1>
        <div class="container">
            <div class="user-info">
                <% if (user) { %>
                    <h2>Welcome, <%= user.firstName %> <%= user.lastName %>!</h2>
                    <p>Email: <%= user.email %></p>
                <% } else { %>
                    <h2>Welcome, Guest!</h2>
                <% } %>
            </div>
            <form action="/active-subscriptions" method="POST">
                <div class="subscription-info"
                <h1>Subscription Details</h1>
                <p><strong>Product Name:</strong> <%= product.name %></p>
                <p><strong>Subscription Duration:</strong> <%= duration %></p>
                <p><strong>Price:</strong> ₹<%= product[duration.replace(/\s/g, '').toLowerCase().replace('months', 'mnths').replace('year', 'year')] || 'Not available' %></p>
            </div>

        <div>
            ,<strong>Subscribed Successfully...</strong>
                <a href="/active-subscriptions" class="btn">Active Subscriptions</a>

        </div>

        </div>
    </div>
    </main>
    <footer class="footer">
        <div class="footer-container">
            <div class="footer-section">
                <h3>FreshMart</h3>
                <p>Bringing fresh produce directly from farms to your table.</p>
            </div>
            <div class="footer-section">
                <h3>Quick Links</h3>
                <ul>
                    <li><a href="products.html">Products</a></li>
                    <li><a href="careers.html">Careers</a></li>
                    <li><a href="contact.html">Contact</a></li>
                    <li><a href="faqs.html">FAQs</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Follow Us</h3>
                <div class="social-links">
                    <a href="#" class="social-link">Facebook</a>
                    <a href="#" class="social-link">Instagram</a>
                    <a href="#" class="social-link">Twitter</a>
                </div>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2024 FreshMart. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>
